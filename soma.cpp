#include "bits/stdc++.h"

using namespace std;

int main()
{
    int x, y, soma = 0;

    cout << "Digite o valor de X: ";
    cin >> x;
    cout << "Digite o valor de y: ";
    cin >> y;

    soma = x + y;

    cout << "SOMA = " << soma;
    return 0; 
}
